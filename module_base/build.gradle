apply {
    from "${rootDir.path}/gradle/config/config_lib.gradle"
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    api rootProject.ext.dep.cc
    api project(':component')
}


